[package]
name = "unillm-kv"
version = "0.1.0"
edition = "2021"
description = "Hybrid KV cache (RadixAttention + PagedAttention) for UniLLM"
license = "MIT"
repository = "https://github.com/cognisoc/unillm"
readme = "../../README.md"

[dependencies]